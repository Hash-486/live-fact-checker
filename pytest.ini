[pytest]
asyncio_mode = auto
# The console-script `pytest` puts the rootdir of the collected tests on
# sys.path, not the repo root, so `import src` failed on a fresh clone and
# only `python -m pytest` (which adds the CWD) worked. Every task step in the
# plan says `pytest -v`; this makes that the true command.
pythonpath = .
